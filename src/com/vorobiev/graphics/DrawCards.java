package com.vorobiev.graphics;

import com.vorobiev.entity.CardRepository;

public class DrawCards extends AbstractDraw {
	//private float lastTime = 0;
	@Override
	public void paint(CardRepository cs, float deltaTime) {
		
		//Card c = cs.whatLastCard();
		//lastTime += deltaTime * 2;
		//if(lastTime > 1) lastTime = 0;
		//AbstractDraw.paint(c, 100, 100, 100 + (int)(6 * lastTime), 140 + (int)(9 * lastTime));
	}
}
